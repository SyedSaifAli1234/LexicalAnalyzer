int: numPrint (int: num, int: length)
{
int : i, j, first, temp;
char : a;
a := ‘x’;
write( "enter number" );
read >> i;
write(i);
i := length;
while (i > 0)
{
first:= 0; /*this line contains a comment*/
j <- 1;
while (j < i)
{
write( j);
j := j + 1;
}
/* this is a comment */
i:= i - 1;
/*This is a
Multiline
Comment*/
}